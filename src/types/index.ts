export type IUser = {
    id: string;
    name: string;
    username: string;
    email: string;
};

export type Employee = {
    id: string,
    firstName: string,
    lastName: string,
    startDate: string,
    department: string,
    birthDate: string,
    street: string,
    city: string,
    state: string,
    postalCode: number,
}