export type User = {
    id: string;
    name: string;
    username: string;
    email: string;
};

export type Employee = {
    id?: string,
    firstName: string,
    lastName: string,
    startDate: string,
    department: string,
    birthDate: string,
    address: string,
    city: string,
    postalCode: number,
}